import { describe, expect, it } from 'vitest'; import { createEmptyAppData } from '../domain/migration'; import { previewMonitoringTargetsCsv } from './monitoringCsv'
const head='company_name,aliases,candidate_status,current_rank,official_recruit_url,recruit_entry_url,mypage_url,monitor_web,sender_domains,gmail_history_found,mypage_found,login_id_found,source_checked_at,notes,work_history_eligibility,eligibility_checked_at'
describe('monitoring CSV import preview',()=>{it('is idempotent and never accepts private columns',()=>{const csv=head+'\nExample,Ex,active,1,https://example.test,,,true,,,,,,,confirmed,';const data=createEmptyAppData();const one=previewMonitoringTargetsCsv(csv,data);const two=previewMonitoringTargetsCsv(csv,{...data,userCompanies:one.candidates});expect(one.candidates).toHaveLength(1);expect(two.candidates).toHaveLength(1);expect(()=>previewMonitoringTargetsCsv('company_name,login_id\nX,secret',data)).toThrow('private')});it('does not overwrite rank or selection because CSV has no evaluation mutation',()=>{const p=previewMonitoringTargetsCsv(head+'\nX,,,,,,,,,,,,,,,bad',createEmptyAppData());expect(p.targets[0].status).toBe('watch')})})
