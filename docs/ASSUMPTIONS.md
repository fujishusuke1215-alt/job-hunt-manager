# AIによる仮定

1. 初版は1人が1つのブラウザーで使う。
2. 本人用データはlocalStorage、公開デモはリポジトリ内の架空データに分離する。
3. 総合点は0〜5の6評価軸から100点換算する。
4. 締切警告は7日以内、期限超過は別表示とする。
5. 日時は利用端末のローカル時刻として扱う。
6. 初版のバックアップ形式は、入れ子の選考データを保てるJSONとする。

いずれも後から変更できる実装にする。

